To have this fastfetch, first you go to your terminal type in fastfetch --gen-config

Then, you might see something like this : Error: file `/home/zilelai/.config/fastfetch/config.jsonc` exists. Use `--gen-config-force` to overwrite

In the next command, type in nano and the filename.

Delete every code and paste the code in the folder fastfetch

Save and type fastfetch again!
